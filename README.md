# GuessingApp

Use Case 3: Hint Generation

Generates contextual hints after incorrect guesses without revealing the answer.
Uses arithmetic logic (even/odd, range, divisibility) to guide the player.
Limits hints to maintain difficulty and game balance.
